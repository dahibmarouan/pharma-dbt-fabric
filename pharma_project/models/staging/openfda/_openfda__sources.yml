version: 2

sources:
  - name: openfda
    description: "Adverse drug event reports (FAERS), extracted via the openFDA API."
    tables:
      - name: raw_adverse_events
        description: "One report per row, raw structure as returned by the API, before any transformation."
        meta:
          external_location: "../data/raw/*.json"